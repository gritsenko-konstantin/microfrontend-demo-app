import { BaseTheme } from '@microfrontend-demo/design-system/styles';

export const Theme = {
    ...BaseTheme,
    colors: {
        ...BaseTheme.colors,
        tio: '#2196F3',
        tio_dark: '#1976D2',
        tio_light: '#BBDEFB'
    }
};

export default Theme;
